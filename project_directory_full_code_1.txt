Aggregated on: 2026-03-13 16:31:43
================================================================================
Project Directory Tree:
/
├── static
│   ├── images
│   ├── js
│   ├── json
│   └── png
│       ├── ship_burners.png
│       └── ship_off.png
├── templates
│   └── index.html
├── .bashrc
├── .gitconfig
├── .gitignore
├── .profile
├── .pythonstartup.py
├── .vimrc
├── _Review_Functions.py
├── flask_app.py
├── general.md
├── project_directory_full_code_1.txt
└── README.txt

6 directories, 14 files
================================================================================

[-] This file: .pythonstartup.py | Contents:
import rlcompleter
import readline
readline.parse_and_bind("tab: complete")
================================================================================

[-] This file: flask_app.py | Contents:
import re
import requests
from flask import Flask, render_template, request, jsonify
app = Flask(__name__)
# Point this directly to your newly built AIManager microservice
AI_MANAGER_URL = "https://aimanager.pythonanywhere.com/process_request"
@app.route('/')
def index():
    return render_template('index.html')
@app.route('/generate_game', methods=['POST'])
def generate_game():
    data = request.json
    description = data.get('description', '').strip()
    if not description:
        return jsonify({"error": "Game description is required"}), 400
    try:
        # Strict system prompt mapped to AIManager's "instructions" parameter
        system_instructions = (
            "You are an expert game developer. Create a single-page HTML game based on the user's description. "
            "Your output must be EXCLUSIVELY valid HTML containing all necessary CSS and JavaScript within it. "
            "Do not include any markdown formatting like ```html, and do not provide any explanations. "
            "The game must be fully playable, robust, self-contained, and visually appealing."
        )
        # Payload perfectly mapped to your AIManager's /process_request endpoint
        payload = {
            "provider": "anthropic",
            "model_key": "claude-sonnet-4-5-20250929",
            "query": description,
            "parameters": {
                "instructions": system_instructions,
                "max_tokens": 8000
            }
        }
        # Send request to AIManager (allowing 120s for Claude to write the code)
        response = requests.post(AI_MANAGER_URL, json=payload, timeout=120)
        # If AIManager returns a 500 or 400, raise it
        response.raise_for_status()
        manager_data = response.json()
        # Extract the code from the AIManager's "outputs" array
        outputs = manager_data.get('outputs', [])
        if not outputs:
            return jsonify({"error": "AIManager returned an empty response."}), 502
        generated_code = outputs[0]
        # Fallback cleanup just in case Claude includes markdown despite instructions
        generated_code = re.sub(r'^```html\s*', '', generated_code)
        generated_code = re.sub(r'^```\s*', '', generated_code)
        generated_code = re.sub(r'\s*```$', '', generated_code)
        return jsonify({"game_html": generated_code.strip()})
    except requests.exceptions.RequestException as e:
        # Catch connection errors, timeouts, or 500s from AIManager
        return jsonify({"error": f"Failed to communicate with AIManager: {str(e)}"}), 502
    except Exception as e:
        return jsonify({"error": f"Internal formatting error: {str(e)}"}), 500
if __name__ == '__main__':
    app.run(debug=True)
================================================================================

[-] This file: templates/index.html | Contents:
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Alien Cargo - Game Generator</title>
    <style>
        body {
            background-color: #121212;
            color: #ffffff;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            display: flex;
            flex-direction: column;
            align-items: center;
            margin: 0;
            padding: 20px;
        }
        h1 {
            color: #00d2ff;
            margin-bottom: 5px;
        }
        p.subtitle {
            color: #aaaaaa;
            margin-bottom: 20px;
        }
        #generator-ui {
            width: 100%;
            max-width: 800px;
            display: flex;
            flex-direction: column;
            gap: 15px;
            margin-bottom: 30px;
        }
        textarea {
            width: 100%;
            height: 120px;
            padding: 15px;
            font-size: 16px;
            font-family: inherit;
            background-color: #1e1e1e;
            color: #ffffff;
            border: 1px solid #333;
            border-radius: 8px;
            resize: vertical;
            box-sizing: border-box;
        }
        textarea:focus {
            outline: none;
            border-color: #00d2ff;
        }
        button {
            padding: 15px;
            font-size: 18px;
            font-weight: bold;
            cursor: pointer;
            background: linear-gradient(135deg, #00d2ff 0%, #3a7bd5 100%);
            color: #ffffff;
            border: none;
            border-radius: 8px;
            transition: opacity 0.2s, transform 0.1s;
        }
        button:hover {
            opacity: 0.9;
        }
        button:active {
            transform: scale(0.98);
        }
        button:disabled {
            background: #555555;
            color: #888888;
            cursor: not-allowed;
            transform: none;
        }
        /* 1280x720 centered container */
        #game-wrapper {
            width: 1280px;
            height: 720px;
            background-color: #000000;
            border: 2px solid #333;
            border-radius: 4px;
            position: relative;
            display: flex;
            justify-content: center;
            align-items: center;
            box-shadow: 0 10px 30px rgba(0,0,0,0.5);
        }
        iframe {
            width: 100%;
            height: 100%;
            border: none;
            border-radius: 4px;
            background-color: #000;
        }
        #loading {
            display: none;
            position: absolute;
            flex-direction: column;
            align-items: center;
            gap: 15px;
            color: #00d2ff;
            font-size: 20px;
            font-weight: bold;
        }
        .spinner {
            width: 40px;
            height: 40px;
            border: 4px solid rgba(0, 210, 255, 0.3);
            border-radius: 50%;
            border-top-color: #00d2ff;
            animation: spin 1s ease-in-out infinite;
        }
        @keyframes spin {
            to { transform: rotate(360deg); }
        }
    </style>
</head>
<body>
    <h1>Alien Cargo 🛸</h1>
    <p class="subtitle">AI-Powered Instant Game Generator</p>
    <div id="generator-ui">
        <textarea id="game-desc" placeholder="Describe your game... (e.g., A top-down space shooter where you defend cargo ships from alien swarms)"></textarea>
        <button id="create-btn">Create Game</button>
    </div>
    <div id="game-wrapper">
        <div id="loading">
            <div class="spinner"></div>
            <span>Compiling game logic...</span>
        </div>
        <iframe id="game-frame" sandbox="allow-scripts allow-same-origin"></iframe>
    </div>
    <script>
        const AI_MANAGER_URL = "https://aimanager.pythonanywhere.com/process_request"; // Ensure this matches your backend
        document.getElementById('create-btn').addEventListener('click', async () => {
            const descInput = document.getElementById('game-desc').value.trim();
            if (!descInput) {
                alert("Please describe your game first!");
                return;
            }
            const btn = document.getElementById('create-btn');
            const loading = document.getElementById('loading');
            const iframe = document.getElementById('game-frame');
            // UI State updates
            btn.disabled = true;
            btn.innerText = "Generating...";
            iframe.style.display = 'none';
            loading.style.display = 'flex';
            iframe.srcdoc = ""; // Clear previous game
            // Construct System Instructions to strictly enforce rules
            const gameInstructions = `You are an expert HTML5 Game Developer. Generate a complete, playable, single-file HTML/JS game.
CRITICAL REQUIREMENTS:
1. The game MUST use a standard HTML5 <canvas> centered in the window with EXACTLY 1280x720 resolution.
2. Controls: Arrow keys to move (WASD for Player 2 if applicable). Enter to start game. Space to shoot/action.
3. It MUST feature a "Game Over" state with a clear visual "Restart / Go Again" button or key-press instruction.
4. Do NOT load external images, audio, or libraries. Use vanilla JS and Canvas 2D API with placeholder shapes/colors (rectangles, circles, path drawing).
5. Output ONLY the raw HTML code starting with <!DOCTYPE html>. Do NOT wrap in markdown \`\`\`html tags. No explanations.`;
            const payload = {
                provider: "anthropic",
                model_key: "claude-sonnet-4-5-20250929", // Reliable for robust single-file code generation
                query: `User's Game Description: ${descInput}`,
                parameters: {
                    task: "text_to_text",
                    instructions: gameInstructions,
                    max_tokens: 8192,
                    budget_tokens: 2048 // Enable thinking for better code architecture
                }
            };
            try {
                const response = await fetch(AI_MANAGER_URL, {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json'
                    },
                    body: JSON.stringify(payload)
                });
                if (!response.ok) {
                    throw new Error(`HTTP error! status: ${response.status}`);
                }
                const data = await response.json();
                let generatedCode = data.outputs[0];
                // Sanitize LLM output in case it still adds markdown formatting
                if (generatedCode.startsWith("```html")) {
                    generatedCode = generatedCode.replace(/```html/g, "").replace(/```/g, "").trim();
                } else if (generatedCode.startsWith("```")) {
                    generatedCode = generatedCode.replace(/```/g, "").trim();
                }
                // Inject code directly into iframe
                iframe.srcdoc = generatedCode;
                iframe.style.display = 'block';
            } catch (error) {
                console.error("Error communicating with AIManager:", error);
                alert("Failed to generate game. Check console for details.");
            } finally {
                // Restore UI State
                btn.disabled = false;
                btn.innerText = "Create Game";
                loading.style.display = 'none';
            }
        });
    </script>
</body>
</html>
================================================================================
